{
  "name": "landing",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && touch docs/.nojekyll",
    "preview": "astro preview",
    "deploy": "astro build && gh-pages -d ../docs",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.10.1"
  }
}
